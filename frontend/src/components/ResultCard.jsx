import { motion } from "framer-motion";
import { FaChartLine } from "react-icons/fa";

export default function ResultCard({ price }) {
  if (!price) return null;

  return (
    <motion.div
      initial={{ opacity: 0, y: 40 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.5 }}
      className="max-w-3xl mx-auto mt-10"
    >
      <div className="bg-slate-900 border border-cyan-500/30 rounded-2xl p-8 shadow-2xl">

        <div className="flex items-center justify-center gap-3 mb-6">
          <FaChartLine className="text-cyan-400 text-3xl" />
          <h2 className="text-3xl font-bold text-white">
            Predicted Price
          </h2>
        </div>

        <h1 className="text-6xl font-extrabold text-cyan-400 text-center">
          ${Number(price).toLocaleString()}
        </h1>

        <p className="text-center text-slate-400 mt-5">
          Estimated market value generated by our machine learning model.
        </p>

      </div>
    </motion.div>
  );
}